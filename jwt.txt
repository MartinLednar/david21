//.env variables
JWT_SECRET=this-is-my-super-secret-string-for-jwt
JWT_EXPIRES_IN=90d
//HOTOVO

//install 
jsonwebtoken
//HOTOVO

//vypytat na zaciatku
const { promisify } = require('util');

//funkcia na vytvorenie jwt
const signToken = (id) => {
  return jwt.sign({ id: id }, process.env.JWT_SECRET, {
    expiresIn: process.env.JWT_EXPIRES_IN,
  });
};
//HOTOVO

//potom na vytvorenie
const token = signToken(newUser._id);
//HOTOVO

//na overenie
const decoded = await promisify(jwt.verify)(token, process.env.JWT_SECRET);