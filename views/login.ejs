<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="/css/style.css" />
    <link rel="shortcut icon" href="/img/logo21SVG.svg" type="image/x-icon" />
    <title>David21 | Admin Log In</title>
  </head>
  <body>
    <div class="login-container">
      <img src="/img/logo21-bg.png" alt="HDB logo" class="logo-login" />

      <div class="login-wrapper">
        <h1 class="section-heading">
          Welcome back!
          <span class="section-heading-upper">Welcome back!</span>
        </h1>

        <%if(errMessage){%>
        <p class="login-error-message">Incorrect password or email !</p>
        <%}%>

        <form method="POST" class="login-form" action="/login">
          <input
            class="section-input"
            type="email"
            name="email"
            placeholder="E-mail"
            required
          />
          <input
            class="section-input"
            type="password"
            name="password"
            placeholder="Password"
            required
          />
          <button class="btn-green" type="submit">Sign In</button>
        </form>
      </div>
    </div>
  </body>
</html>
